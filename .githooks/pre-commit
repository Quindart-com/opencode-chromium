#!/bin/sh
# Best-effort local secret scan before each commit. Skips silently when
# gitleaks is not installed; GitHub secret scanning protects remote pushes,
# and the release workflow enforces the repository hygiene checks.
if ! command -v gitleaks >/dev/null 2>&1; then
  exit 0
fi
exec gitleaks protect --staged --redact -v
